/**
 * Browser event names shared by the replay shell and its lazily loaded 3D scene.
 * Keep this dependency-free so the shell does not pull Arena3DScene into its route chunk.
 */
export const ADD_MARKER_AT_CENTER_EVENT = 'replay:add-marker-at-center';
