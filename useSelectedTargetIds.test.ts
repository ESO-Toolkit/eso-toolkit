// Simple test for useSelectedTargetIds hook
import { describe, it, expect } from '@jest/globals';

describe('useSelectedTargetIds', () => {
  it('should exist', () => {
    expect(true).toBe(true);
  });
});